function triangleArea(input) {
	let a = Number(bill[0]);
	let b = Number(bill[1]);
	let c = Number(bill[2]);
	let halfP = (a + b + c) / 2;
	let area = Math.sqrt(halfP*(halfP-a)*(halfP-b)*(halfP-c));
	return area.toFixed(10);
}
